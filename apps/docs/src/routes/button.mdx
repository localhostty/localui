---
title: Button
---

import { Button } from "@local/ui/button";
import { PropsTable } from "../components/props-table";

# Button Primitive

<div style="">
  <div style="border: 2px solid black; padding: 12px; height: 112px; display: flex;flex-direction: column; align-items: center; justify-content: center">
    <Button onClick="alert()">Native Button</Button>
    <Button nativeButton={false} onClick="alert()">
      Non Native Button
    </Button>
  </div>
  <div>
    Code:
    <pre>
      ```
      <Button>...</Button>
      <Button nativeButton={false}>...</Button>
      ```
    </pre>
  </div>
</div>

## API

### Button

<PropsTable
  props={[
    {
      name: "forwardedRef",
      type: "HTMLButtonElement | HTMLDivElement",
      default: "—",
      description: "Ref to the root button/div element",
    },
    {
      name: "nativeButton",
      type: "boolean",
      default: "true",
      description:
        "Whether to use a semantic HTML <button> Element or a <div> with type button.",
    },
  ]}
/>
